# Play Console 非推奨事項の調査レポート

調査日: 2025-11-02  
調査者: 作業エージェント

## 警告内容

### 1. エッジツーエッジ表示の有効化

**警告内容:**
> 一部のユーザーでエッジ ツー エッジ表示が有効にならないことがあります
> Android 15 以降では、SDK 35 をターゲットとするアプリがデフォルトでエッジ ツー エッジ表示になります。SDK 35 をターゲットとするアプリは、Android 15 以降でアプリが正しく表示されるように、インセットを処理する必要があります。

**現状分析:**
- `MainActivity.kt` で `enableEdgeToEdge()` を呼び出し済み（L19）
- targetSdk = 36 に設定済み
- 既存タスク（tasks.md）でエッジツーエッジ対応は完了済み（🟩）
- エッジツーエッジドキュメント `docs/edge-to-edge.md` 存在

**対応状況:** ✅ 対応済み（前回タスクで完了）

---

### 2. 非推奨APIの使用

**警告内容:**
> お客様のアプリは、エッジ ツー エッジで非推奨の API またはパラメータを使用しています
> - `android.view.Window.setStatusBarColor`
> - `android.view.Window.setNavigationBarColor`
> これらは次の場所で開始します:
> - `com.google.android.material.datepicker.m.I`

**調査結果:**
1. アプリコード内での直接使用: **なし**
   - `grep` 検索の結果、アプリコードには該当APIの使用なし
   
2. 依存ライブラリ内での使用:
   - `com.google.android.material.datepicker.*` から呼び出されている
   - Material Components ライブラリ（`com.google.android.material:material`）内部で使用
   - 現在の依存関係を確認する必要がある

**推奨対応:**
- Material Components ライブラリを最新版に更新
- DatePicker を使用している箇所があれば、Material3 の DatePicker に移行を検討
- アプリコード内では問題なし

---

## 次のステップ

### 非推奨API対応
1. `gradle/libs.versions.toml` で Material ライブラリのバージョンを確認
2. Material Components を最新版に更新
3. DatePicker の使用箇所を確認し、必要に応じて Material3 版に移行
4. ビルドとテストで動作確認

### エッジツーエッジ
- 既に対応済みのため追加作業なし
- テストで動作確認のみ実施

---

## 関連ファイル
- `app/build.gradle.kts`: 依存関係定義
- `gradle/libs.versions.toml`: バージョン管理
- `app/src/main/java/com/mapshoppinglist/MainActivity.kt`: enableEdgeToEdge呼び出し
